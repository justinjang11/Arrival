Read /docs/ARRIVAL_PRODUCT_SPEC.md in full before making changes.

It is the authoritative product specification. Preserve every applicable
BINDING_MVP requirement and acceptance criterion. Do not implement or claim
NORTH_STAR features unless this task explicitly promotes one. Do not silently
resolve UNRESOLVED items. Do not implement OUT_OF_SCOPE features.

Inspect the existing repository before editing. Extend the current architecture
rather than recreating the application. Limit changes to the task below,
document any schema or environment-variable changes, and verify the affected
user journey against the specification before finishing.

TASK: Establish Arrival’s canonical application foundation.

1. Initialize a production-quality Next.js application using the App Router,
   TypeScript, Tailwind CSS, and ESLint in the existing repository.
2. Preserve /docs/ARRIVAL_PRODUCT_SPEC.md unchanged.
3. Create a minimal responsive Arrival landing screen confirming that the
   application is running. Do not design or implement the substantive product
   interface yet.
4. Establish directories for:
   - app routes
   - reusable UI components
   - product features
   - domain logic
   - controlled mock data
   - tests
5. Add:
   - .env.example containing variable names only, never credentials
   - development and build scripts
   - unit-test infrastructure
   - one rendering smoke test
   - a useful README with local setup, testing, and build instructions
   - documentation explaining that future database changes must use committed,
     sequential migrations
6. Do not add Supabase, external APIs, product scraping, AI recommendation
   calls, checkout automation, payment functionality, or sample implementations
   of unresolved product decisions.
7. Run and report:
   - dependency installation
   - lint
   - tests
   - production build

Before finishing, list every file created or changed and explain any temporary
technical assumptions. Do not commit generated secrets, build output, or
dependency directories.